# --- nested independent repo (own remote: github.com/Frielon/inference.git) ---
data_pipeline/

# --- secrets ---
.envrc
*.env

# --- training artifacts / generated outputs ---
logs/
ckpt/
checkpoints/
outputs/
hint_step_outputs_backup/
wandb/
*.console.log
.runtime_env.*.yaml

# --- dataset backups (keep the real parquets, drop the .bak copies) ---
*.bak

# --- python ---
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# --- claude code local settings ---
.claude/settings.local.json

# --- editor / os ---
.DS_Store
.vscode/
.idea/

# --- selector json outputs ---
selector/**/*.json

selector/**/**/*.json

dataset/

# --- large / generated artifacts (regenerable, multi-GB; never commit) ---
# merged checkpoint weights + tokenizers (each shard ~4.7GB)
eval/merged/
# eval sample dumps (samples.jsonl, tens of MB each)
eval/results/
# selector hint-selection test/label outputs (incl. results.jsonl aggregates)
selector/test_cite/results/
selector/test_cite/.codex_label_sessions/
selector/test_cite/gpt_oss_eval/results/
selector/test_cite/gpt_oss_eval/logs/
# catch-all for large model-weight / binary artifacts anywhere in the tree
*.safetensors
*.bin
*.pt
*.ckpt
*.gguf